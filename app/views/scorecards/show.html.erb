<%= render @scorecard.exercise %>

<div class="row">
  <div class="well">
  <% if @scorecard.score4.nil? %>
    <p>
      It looks like you've not set any scores yet. Why don't you see how fast
      you can play this exercise for 4 bars without making any mistakes? Grab
      a metronome, a pair of sticks and your surface of choice and go!
    </p>
  <% elsif @scorecard.score60.nil? %>
    <p>
      So you've proven you can play this exercise for 4 bars. Why not test your
      abilities a bit further? Try setting a score for 60 seconds with no mistakes!
    </p>
  <% elsif @scorecard.score5.nil? %>
    <p>
      You've proven your mettle over 60 seconds, but, can you keep this up for
      the long haul? Try setting a score for 5 minutes with NO MISTAKES!
      This one is tough!
    </p>
  <% else %>
    <p>
      Practise makes perfect! Set a new high score!
    </p>
  <% end %>
  <p>
    I give up on this one, <%= link_to "give me a new one", assign_new_daily_path %>, or 
      <%= link_to "browse all exercises", exercises_path %>.
  </p>
  </div>
</div>

<div class="row">
  <div class="control-group">
    <% if @scorecard.score4.blank? %>
      <div class="col-md-4"></div>
    <% elsif @scorecard.score60.blank? %>
      <div class="col-md-2"></div>
    <% end %>
    <div class="col-md-4">
      <div class="input-group input-group-lg">
        <span class="input-group-addon">4 bars</span>
        <%= form_tag("/users/#{current_user.id}/scorecards/#{@scorecard.id}", 
                      method: :patch) do %>
          <%= text_field_tag :score4, nil, class: "form-control", placeholder: "#{@scorecard.score4}"  %>
          <%= submit_tag "SCORE", class: "btn btn-default btn-danger" %>
        <% end %>
      </div>  
    </div>
    <% unless @scorecard.score4.blank? %>
      <div class="col-md-4">
        <div class="input-group input-group-lg">
          <span class="input-group-addon">60 secs</span>
          <%= form_tag("/users/#{current_user.id}/scorecards/#{@scorecard.id}", 
                        method: :patch) do %>
            <%= text_field_tag :score60, nil, class: "form-control", placeholder: "#{@scorecard.score60}"  %>
            <%= submit_tag "SCORE", class: "btn btn-default btn-danger" %>
          <% end %>
        </div>  
      </div>
    <% end %>
    <% unless @scorecard.score4.blank? || @scorecard.score60.blank? %>
      <div class="col-md-4">
        <div class="input-group input-group-lg">
          <span class="input-group-addon">5 mins</span>
          <%= form_tag("/users/#{current_user.id}/scorecards/#{@scorecard.id}", 
                        method: :patch) do %>
            <%= text_field_tag :score5, nil, class: "form-control", placeholder: "#{@scorecard.score5}"  %>
            <%= submit_tag "SCORE", class: "btn btn-default btn-danger" %>
          <% end %>
        </div>  
      </div>
    <% end %>
  </div>
</div>

<div class="row">
  <div class="control-group"></div>
    <div class="col-md-4 col-md-offset-4 center top-padding">
      <p>
        <%= link_to "RESET scores", reset_scorecard_path, class: "btn btn-lrg btn-danger" %>  
      </p>
    </div>
  </div>
</div>




